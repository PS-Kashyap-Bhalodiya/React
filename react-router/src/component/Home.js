function Home() {
    return <>
        <h1>Home page</h1>

        {/* used to build a single page applications.
            Application that have many pages but the page is never refreshed instead the content is dynamically 
            fetched based on the URL, this process is called Routing.
        */}
    </>
}

export default Home;